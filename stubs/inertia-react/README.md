# Laravel Inertia DataTables React Stubs

This folder is a publish target placeholder for optional React/Inertia starter files.

Backend usage does not require these files. Future stubs published with `inertia-datatables-react` may need app-specific imports, UI primitives, Tailwind classes, Inertia helpers, and route helpers.
